import 'package:flutter/material.dart';

class TextStyles {
  static TextStyle eewTitleStyle({
    Color? textColor,
  }) =>
      TextStyle(
        fontSize: 18,
        fontWeight: FontWeight.bold,
        color: textColor,
      );
}
