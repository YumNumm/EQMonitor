name: jma_parameter_types
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  protobuf: ^5.1.0

dev_dependencies:
  altive_lints: ^2.1.0
  eqmonitor_lints:
    path: ../eqmonitor_lints
  test: ^1.29.0
