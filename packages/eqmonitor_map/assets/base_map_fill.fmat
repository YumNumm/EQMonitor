material {
  name: "BaseMapFill",
  shading_model: unlit,
  blending: opaque,
  culling: none,
  parameters: [
    { type: vec4, name: fill_color, hint: source_color, default: [1.0, 1.0, 1.0, 1.0] },
  ],
}

fragment {
  void Surface(inout MaterialInputs material) {
    material.base_color = material_params.fill_color;
    PrepareMaterial(material);
  }
}
