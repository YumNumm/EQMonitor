name: jma_code_table_types
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  protobuf: ^6.0.0

dev_dependencies:
  altive_lints: ^2.1.0
  eqmonitor_lints:
    path: ../eqmonitor_lints
  test: ^1.29.0
