name: jma_code_table_converter_internal
description: A sample command-line application.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  archive: ^4.0.7
  crypto: ^3.0.7
  html: ^0.15.6
  http: ^1.2.1
  jma_code_table_types:
    git:
      url: https://github.com/YumNumm/EQMonitor.git
      path: packages/eqapi_types

dev_dependencies:
  altive_lints: ^2.1.0
  eqmonitor_lints:
    path: ../eqmonitor_lints
  test: ^1.29.0
