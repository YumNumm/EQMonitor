name: pmtiles_v3
description: PMTiles v3 archive形式のrandom-access readerを提供する純Dart package。
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace
dependencies:
  freezed_annotation: ^3.1.0
dev_dependencies:
  build_runner: ^2.16.0
  eqmonitor_lints:
    path: ../eqmonitor_lints
  freezed: ^3.2.6-dev.1
  test: ^1.31.1
