name: workflows
description: Durable client-side workflow runner with step-level idempotency and persistence.
version: 1.0.0

publish_to: "none"

environment:
  sdk: ^3.11.0

resolution: workspace

dev_dependencies:
  eqmonitor_lints:
    path: ../eqmonitor_lints
  test: ^1.29.0
