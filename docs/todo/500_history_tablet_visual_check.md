# 履歴2画面のタブレット表示確認

2026-09-10: コードレビュー・静的解析を実施。回帰テストはユーザー指定で追加・実行しない。
この作業環境ではブラウザ接続が利用不可、ネイティブUI操作も起動に失敗したため、実機表示は未確認。
一時的なWebプレビューもビルド完了には至らず、表示確認の根拠にはしていない。

端末またはシミュレータが利用可能になったら、以下を手動で確認する。

- 839/840dpの境界、タブレット縦横、OSの分割表示。
- イベント選択後の幅変更で選択、一覧位置、地図、シート状態が維持されること。
- 縦・横ヒンジ、幅0の折り目、片側が狭い場合に表示と操作がヒンジに重ならないこと。
- 読み込み・失敗・空一覧と詳細の再試行。読み込み中のスクロール操作。
- 文字拡大・Light/Dark・一覧の選択枠・EEW各報の横スクロール。
- 別イベント選択・詳細を閉じる操作でEEW再生が停止すること。
- 地図のタップ、ズーム、詳細シート展開、戻る操作、既存の詳細URL。
